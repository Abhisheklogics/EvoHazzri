/**
 * @format
 */

import {AppRegistry} from 'react-native';

import {name as appName} from './app.json';
import Section from './src/App.tsx';

AppRegistry.registerComponent(appName, () => Section);
